import { CenteredLayout } from "@/layouts";
import { LoginContainer } from "@/pods/login/LoginContainer";
import React from "react";

export const LoginPage = () => {
  return (
    <CenteredLayout>
      <LoginContainer />
    </CenteredLayout>
  );
};

import { CenteredLayout } from "@/layouts";
import { ListContainer } from "@/pods/list/ListContainer";
import React from "react";

export const ListPage = () => {
  return (
    <CenteredLayout>
      <ListContainer />
    </CenteredLayout>
  );
};
